# Implementation Plans

Generated by the improve skill on 2026-06-10 against commit `7958670b` (branch
`iniciando_desarrollo_para_produccion`). Execute in the order below unless dependencies
say otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

Selection note: this run was non-interactive, so the top findings by leverage were
planned by default (the skill's documented fallback). The full findings table from the
audit is in the session report; anything below in "considered and rejected / deferred"
can be promoted to a plan on request.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Enforce real authentication on documentos and storage endpoints | P1 | M | — | DONE — commit `417cba64`, branch `bugfix/enforce-documentos-auth` |
| 002 | Add idempotency guard to pre-incapacidad promotion | P1 | S | — | DONE — commit `052cb65b`, branch `bugfix/promotion-idempotency-guard` |
| 003 | Make pre-incapacidad promotion atomic (single commit) | P2 | M | 002 | DONE — commit `e2f0491a`, branch `bugfix/promotion-single-transaction` |
| 004 | Restore a minimal CI pipeline | P1 | M | — | BLOCKED: awaiting push — workflow cannot be fully verified until pushed to GitHub |
| 005 | Patch known-vulnerable npm dependencies in both frontends | P2 | S | — | DONE — commit `be9d6a21`, branch `bugfix/npm-audit-frontends` |
| 006 | Parameterize default credentials and harden production config | P2 | S | — | TODO |
| 007 | Add flush-only variants to BaseRepository (Plan 003 prerequisite) | P2 | S | — | DONE — commit `3d8880f7`, branch `bugfix/base-repository-flush-variants` |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 003 requires 007 (new): `BaseRepository.create/update` commit internally, defeating the
  single-transaction approach. Plan 007 adds `create_flushed`/`update_flushed` variants and
  exposes `flush_only=False` params on the two affected service methods. Plan 003 then passes
  `flush_only=True` to those calls.
- 003 requires 002: the single-transaction refactor assumes the idempotency guard exists
  (safe retries after the new rollback paths), and both edit the same method — landing 002
  first keeps diffs reviewable.
- 001 and 004 interact softly: once CI (004) exists, 001's expanded test suite runs on
  every push. No hard ordering.
- 001 touches `DocumentosViewer.tsx`, which had uncommitted local modifications at
  planning time — its executor must STOP if the working tree is still dirty there.

## Findings considered and rejected / deferred

Rejected (do not re-audit):

- **DocumentosViewer useEffect "infinite loop"** (`[documentos, previewUrls]` deps): the `!previewUrls.has()` guard terminates after one extra pass — minor inefficiency, not a bug. Not worth a plan; incidentally restructured by Plan 001 Step 5 anyway.
- **"No integration tests for unified promotion"**: wrong — `tests/test_pre_incapacidad_promotion_service.py` (317 lines) is DB-backed and covers the unified flow incl. document copy. The real residual gaps (idempotency, failure atomicity) are folded into Plans 002/003.
- **listar_pendientes timezone bug**: already fixed in commit `b986075c`.
- **CSRF protection**: bearer-token-in-header auth is not CSRF-exploitable; by-design for this architecture.
- **React Query staleTime tuning / HTTP cache headers on authed lists**: preference-level tuning, negligible payoff at current scale.
- **Bulk CRUD endpoints for incapacidades**: missing DELETE/bulk ops are plausibly by design (audit-trail preservation); no demand evidence.
- **React Query cache-lifecycle test suite**: generic suggestion, not grounded in an observed defect.
- **package.json version "0.0.0"**: cosmetic; not worth executor time now.

Deferred (real, but not planned in this batch — promote on request):

- **Rate limiting on public filing endpoints** (`/pre-incapacidades/radicar`, uploads, public download): confirmed absent; M effort (SlowAPI + Redis). Do before public launch.
- **RBAC sweep of remaining internal endpoints**: `incapacidades.py` ~814/869/894 and internal `pre_incapacidades.py` routes authenticate but apply no `PermissionChecker`; same pattern as Plan 001 Step 3. Also per-object ownership (IDOR) for EMPRESA/EMPLEADO roles is an unsolved design question.
- **Promotion-task reconciliation sweeper**: radicar endpoint swallows enqueue failures (`pre_incapacidades.py:71-77`) → pre-incapacidades can sit in PENDIENTE forever. Safe to build only after 002+003.
- **Null-solicitante guard in email task** (`incapacidad_tasks.py:185-186`): real but the failure is already swallowed (email silently skipped); small fix, batch with the sweeper work.
- **Python dependency refresh** (fastapi 0.109/sqlalchemy 2.0.25/asyncpg 0.29, early-2024): run `pip-audit`, then a deliberate upgrade plan; not an audit-fix one-liner.
- **Backend god-modules** (`incapacidad_service.py` ~1,310 LOC; `incapacidades.py` endpoint file ~1,085 LOC): real debt, but L effort/HIGH risk on the workflow core — do after CI + characterization coverage mature.
- **Docs reconciliation**: CLAUDE.md says "11 tables" (now 13+ with `pre_incapacidades`, `validation_inconsistencias`); state-machine docs don't mention the pre-incapacidad flow; `docs/` root has ~23 mixed-vintage files and `ai/skills/` has a `frontend-design`/`frontenddesign` duplicate. Cheap, low urgency.
- **Production direction items**: notification/report Celery tasks are TODO stubs (`notification_tasks.py`, `report_tasks.py`); no deployment runbook/backup/monitoring story in `infra/` despite the production-bound branch; sistema-interno pages `BandejaPage`/`GestionarPreIncapacidadPage` have zero component tests.
